image/*; cp %s $MTMPDIR \; edisplay $MTMPDIR/$(basename %s) &
text/html; cp %s $MTMPDIR \;  luakit $MTMPDIR/$(basename %s) &; nametemplate=%s.html;
application/pdf; cp %s $MTMPDIR \; mupdf $MTMPDIR/$(basename %s) &
application/*; cp %s $MTMPDIR \; xdg-open $MTMPDIR/$(basename %s) &
